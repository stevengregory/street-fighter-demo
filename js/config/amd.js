// require js config
require.config({
    deps: ['fighter', '../main'],
    paths: {
        'jquery': '../../bower_components/jquery/dist/jquery.min',
        'character': '../modules/character'
    }
});